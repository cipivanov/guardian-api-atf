Feature: the version can be retrieved

  Scenario: client makes call to GET /version
    Given user provides numbers 1 and 2
    When computer adds the numbers received
    Then computer obtains 3 as a result